<h1>All The Bands</h1>

<% @bands.each do |band| %>
  <ul>
    <li>
      <a href="<%=band_url(band)%>"><%= band.name %></a>
    </li>
  </ul>
  <br>
<% end %>
<a href="<%=new_band_url%>">Add A Band</a>
